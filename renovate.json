{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "dependencyDashboard": true,
  "rangeStrategy": "pin",
  "customManagers": [
    {
      "customType": "regex",
      "description": "npm packages pinned in MCP server args, e.g. @azure/mcp@2.0.5",
      "managerFilePatterns": ["/(^|/)\\.mcp\\.json$/"],
      "matchStrings": [
        "\"(?<depName>(?:@[a-z0-9-]+/)?[a-z0-9.-]+)@(?<currentValue>\\d+\\.\\d+\\.\\d+[^\"]*)\""
      ],
      "datasourceTemplate": "npm"
    }
  ],
  "packageRules": [
    {
      "description": "Plugin files change: the versioning rule applies",
      "matchFileNames": ["*/**"],
      "matchManagers": ["custom.regex"],
      "prBodyNotes": [
        "This changes a plugin folder. Before merging, bump `version` in that plugin's `.claude-plugin/plugin.json` and add a `CHANGELOG.md` entry."
      ]
    }
  ]
}
